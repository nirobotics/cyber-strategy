# 年度赛季需求说明

这份文档用于在每年创建 Cyber Strategy 新赛季版本前，把需求完整地告诉 AI。它回答“今年需要做什么”；具体代码放在哪里、怎样实现和怎样验收，见 [`SEASON_CUSTOMIZATION.md`](./SEASON_CUSTOMIZATION.md)。

你不需要先了解代码。只需要把“今年比赛是什么、数据长什么样、页面想显示什么”说明白，AI 会负责检查仓库并提出实现计划。

## 最少先准备这 5 样东西

如果资料还没全部公布，可以先提供下面 5 项，让 AI 开始盘点：

1. **年份、分支和己方队号**：例如目标分支叫 `strategy-2027`，己方队伍是哪些。
2. **官方规则链接**：Game Manual、FIRST API 文档、TBA 当年 Score Breakdown 文档；只有 PDF 也可以直接提供文件。
3. **Cyber Scout 代码**：告诉 AI 代码在哪里；下面有不需要懂代码的提供方法。
4. **当年数据和计算逻辑**：把当年的字段说明、计算规则、表格或现有代码一起提供。
5. **还没确定的内容**：直接写 `TBD`。例如场地图未发布，就写“场地图 TBD，先保持空背景”。

不要提供 API key、service role key、OAuth secret、用户 token 或其他密钥。只需要说明相应环境变量是否已经配置，不要提供值。

### 怎样把 Cyber Scout 代码交给 AI

任选一种最方便的方式：

- 代码在同一台电脑：告诉 AI 文件夹的完整路径，或者把文件夹复制到当前工作区旁边。
- 代码在 GitHub：提供仓库链接，并确保当前 Codex 环境有读取权限。
- 只有压缩包：把压缩包放进当前工作区，再告诉 AI 文件名。

你不需要自己查表名、字段类型或制作 JSON。AI 读取代码后会整理这些信息；如果代码里仍找不到某项，AI 必须用容易回答的问题单独询问，不能让你先完成技术分析。

### 每年必须由人提供的内容

每个赛季开始时，请明确提供以下材料：

1. 当年使用的 Cyber Scout 代码或代码位置。
2. 当年会采集的全部数据及含义。
3. 当年使用的全部计算逻辑，包括计分、指标、预测和特殊判定。

材料可以是文档、表格、代码或文字说明，不要求固定格式。AI 负责把材料转换成字段映射和实现方案。如果 AI 还需要这三类以外的内容，必须先说明“缺什么、为什么需要、怎样提供”，再由人判断是否提供。

## 完整信息清单

### 1. 新赛季的基本信息

告诉 AI：

- 赛季年份和官方名称。
- 新分支的准确名称。
- 默认从 `strategy-template` 最新的 commit 开始；只有明确指定时才使用其他 commit。
- 新年度版本号从多少开始；没有约定时让 AI 先提出建议。
- 己方队号；如果系统要同时服务多支队伍，把全部队号列出来。
- 默认赛事或 event key；还不知道可以写 `TBD`，以后再补。

### 2. 官方比赛规则和计分方式

优先提供官方链接或文件，不要只用口头总结。需要说明：

- 一场比赛总时长，以及 Auto、Teleop、Endgame 分别从什么时候开始和结束。
- 当年所有得分动作，每个动作的分值和成立条件。
- FIRST FRC Events 返回哪些当年字段。
- TBA `score_breakdown` 中对应字段叫什么。
- 联盟总分怎样分配或估算到单支队伍；不能准确分配时使用什么估算方式。
- Qualification、Playoff 或其他比赛阶段是否使用不同规则。
- 哪些规则仍未确定，哪些资料是正式版，哪些只是草案。

### 3. Cyber Scout 和当年计算材料

把上面“每年必须由人提供的内容”交给 AI，并补充说明：

- Normal Scout、Super Scout、Pit Scout 当年分别记录什么。
- 哪些数据允许在 Strategy 中修改，以及谁可以修改。
- 是否需要兼容上一年的数据。

到这里即可。表名、字段类型、记录结构、空值和旧格式由 AI 从代码和材料中整理，不要求你手工填写。AI 整理后必须给出一份容易核对的中文清单，让你只确认业务含义和计算结果。

### 4. 页面要展示哪些年度指标

每个指标单独说明：

- **key**：写入代码和历史数据的稳定英文 key，确定后不要随意改名。
- **名称**：页面上显示的中文名称。
- **来源**：Normal、Super、Pit、FIRST、TBA，或由多个字段计算。
- **公式**：总和、平均值、百分比、成功次数/尝试次数，还是其他算法。
- **格式**：单位、小数位、是否显示百分号。
- **位置**：是否出现在队伍摘要、逐场表格、队伍对比、图表或 Scout 编辑器。
- **缺失值处理**：显示 `0`、`—`，还是不参与平均值。

例如，只说“显示准确率”还不够。应该说明“成功次数 ÷ 尝试次数，尝试次数为 0 时显示 `—`”。

### 5. 计分数据的来源和降级顺序

模板默认优先级是 FIRST FRC Events → TBA 年度 breakdown → Super Scout 估算 → `0`。需要说明：

- FIRST 返回的总分、Auto、Teleop 和当年专用字段分别是什么。
- TBA 中与这些字段对应的 key。
- Super Scout 用哪些字段估算单队得分，以及计算公式。
- 某个来源缺失、比赛未结束或字段为 `null` 时，什么时候进入下一级来源。
- 页面是否要标注最终使用了哪个数据源。

如果今年要改变默认优先级，必须说明原因和新的顺序。

### 6. 机器人状态和宕机判定

模板只有三个状态，并固定沿用当前判断方式，不需要每年提供：

- Cyber Scout 的 `noShow` 为真时显示“未到场”。
- `disabled` 为真，或宕机累计时间超过 20 秒时显示“宕机”。
- 其他情况显示“正常”。

状态同时出现时，优先级固定为：未到场 → 宕机 → 正常。

### 7. 自动路线和策略画板

自动路线与策略画板可以使用不同背景图。每张图分别提供：

- 图片文件，或仓库中已经存在的准确路径。
- 图片来源、授权方式和是否允许放进仓库。
- 场地实际方向和蓝/红联盟方向。
- 宽高比。
- 节点、机器人、起始站位等百分比坐标。
- PDF 和 Cyber Pit Embed 是否使用同一张图。

场地图或坐标没确定时写 `TBD`。AI 应继续使用中性网格和“未配置年度场地图”，不能生成假地图或假坐标代替。

### 8. Strategy Proposal 的年度阶段

这里的“阶段”是指一场比赛里的阶段，例如 Auto、Teleop、Endgame；不是 Proposal 的草稿、待审核、已通过等流程状态。

每个阶段都要给出：

- 稳定 ID，例如供代码和旧数据识别的英文值。
- 页面显示名称。
- 展示顺序。
- 该阶段要讨论或记录什么。
- 是否影响 PDF 输出、审核流程或画板初始状态。

还要说明是否继续兼容旧 Proposal 数据；默认应保留旧 `auto`、`self_strategy`、`partner_strategy` payload 的读取能力。

### 9. 今年要增加、保留或关闭的功能

不要只说“按今年规则改”。按页面列出变化更清楚，例如：

- 队伍摘要新增什么、移除什么。
- 逐场表格增加哪些年度列。
- Match Analysis 是否需要新预测或新状态。
- Picklist 是否保持原样。
- Proposal、Scouting Lead、管理设置、Cyber Pit Embed 是否保持原样。
- 是否需要兼容去年的数据库记录、本地存储和 Proposal JSON。

没有变化的功能也可以写“保持模板默认”，这样 AI 不会误删。

### 10. 怎样才算完成

你只需要用实际使用方式说明结果，不需要自己设计测试。至少回答：

- **结果对不对**：给出 1–3 个你已经知道答案的例子，例如“输入这些比赛数据后，队伍总分应为 42”。
- **哪些操作必须能用**：列出本赛季最重要的页面或操作，例如查看队伍、生成 Proposal PDF、使用 Picklist。
- **特殊情况怎样显示**：例如没有数据时显示 `—`，未提供场地图时显示空网格。
- **交付到哪一步**：只改代码，还是还要 commit、push 或部署；未明确允许时，AI 不执行后三项。

其余检查默认由 AI 负责：运行类型检查、lint、测试和构建，并检查主要页面的桌面/手机、浅色/深色显示。AI 必须先把具体验收清单给你确认。

## 可直接复制给 AI 的完整模板

把方括号内容替换成当年的信息；暂时不知道的地方写 `TBD`，不要删除该项。

```text
我要基于 Cyber Strategy Template 创建一个新赛季版本。

一、分支与基本信息
- 赛季年份和名称：[填写]
- 目标分支名：[例如 strategy-20XX]
- strategy-template 起始 commit：默认使用最新 commit；如需例外请填写 [commit]
- 起始版本号：[填写或“请先建议”]
- 己方队伍：[填写全部队号]
- 默认赛事 / event key：[填写或 TBD]

二、官方资料
- Game Manual：[链接或文件]
- FIRST FRC Events 文档：[链接或文件]
- TBA Score Breakdown 文档：[链接或文件]
- 比赛总时长和 Auto / Teleop / Endgame 时间：[填写]
- 当年得分动作、分值和成立条件：[填写]
- Qualification / Playoff 的规则差异：[填写或“无”]
- 仍处于草案或尚未确定的规则：[填写或“无”]

三、Cyber Scout 数据
- Cyber Scout 代码位置：[文件夹路径 / GitHub 链接 / 工作区内压缩包]
- Normal / Super / Pit 当年分别记录什么：[填写]
- 当年全部数据说明和计算逻辑的位置：[文档 / 表格 / 代码 / 文字说明]
- Strategy 中允许修改的内容和人员：[填写或“保持模板默认”]
- 是否兼容上一年数据：[是 / 否 / 请 AI 评估]
- 请 AI 从代码中整理表名、字段、结构、空值和旧格式；缺少其他材料时先说明原因再询问。

四、年度指标
请为每个指标填写：key / 中文名称 / 来源字段 / 公式 / 单位 / 小数位 / 展示页面 / 缺失值处理。
- 指标 1：[填写]
- 指标 2：[填写]
- 其他指标：[填写]

五、计分与降级
- FIRST 总分、Auto、Teleop 和年度字段映射：[填写]
- TBA score_breakdown 字段映射：[填写]
- 联盟数据分配到单队的规则：[填写]
- Super Scout 估算公式：[填写]
- 缺失、null、未完赛时的降级条件：[填写]
- 是否沿用 FIRST → TBA → Super Scout → 0：[是 / 否；否时填写新顺序和原因]

六、机器人状态
- 沿用模板固定判断，不需要填写：`noShow` 为真是未到场；`disabled` 为真或宕机累计时间超过 20 秒是宕机；其余是正常。
- 状态优先级为未到场 → 宕机 → 正常。

七、场地和坐标
- 自动路线背景图、来源授权、方向、宽高比、节点和初始坐标：[填写或 TBD]
- 策略画板背景图、来源授权、方向、宽高比、节点和初始坐标：[填写或 TBD]
- PDF / Cyber Pit Embed 的背景要求：[填写]
- 如果是 TBD，请保持中性网格，不要制作假地图或假坐标。

八、Proposal 阶段
- 一场比赛包含哪些阶段（如 Auto / Teleop / Endgame）、顺序和用途：[逐项填写]
- PDF、审核或画板的特殊要求：[填写或“无”]
- 旧 Proposal payload 兼容要求：[填写；默认全部保留读取]

九、功能范围
- 必须新增：[填写]
- 必须保留：[填写]
- 必须关闭或移除：[填写]
- 保持模板默认、不需要改动：[填写]
- 历史数据库、Proposal JSON 和本地存储兼容要求：[填写]

十、验收与交付
- 已知输入和预期结果的例子：[填写 1–3 个]
- 本赛季最重要、必须能用的页面或操作：[填写]
- 无数据、无场地图等特殊情况应怎样显示：[填写]
- 本轮是否允许 commit / push / 部署：[分别填写允许或不允许]

请按以下顺序工作：
1. 先阅读仓库规则、README、SEASON_REQUIREMENTS.md 和 SEASON_CUSTOMIZATION.md，并检查现有代码。
2. 输出“已收到的信息”“缺失或冲突的信息”“字段映射表”和实施计划。
3. 对所有 TBD 保持未配置状态，不猜测规则、字段、图片或坐标。
4. 等我确认计划后再修改代码。
5. 修改完成后运行项目要求的测试并报告结果。
6. 未经我明确同意，不要 commit、push 或部署。
```

## AI 第一阶段应该交付什么

收到以上资料后，AI 不应立即写代码。第一阶段应先提供：

1. **信息确认表**：哪些内容已明确，哪些是 `TBD`。
2. **缺失和冲突清单**：例如文档字段名与 Scout payload 不一致。
3. **字段映射表**：原始字段如何进入通用字段、年度指标和页面。
4. **实现计划**：会修改哪些 `app/season/` 文件、资源和测试。
5. **明确不改的内容**：避免为了新赛季误改通用 Picklist、权限或历史数据兼容逻辑。

确认这五项后，再让 AI 开始实现。
